<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DataExample"
             x:Class="DataExample.MenuSupervisor">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Order="Primary" Text="Delete" Clicked="Delete_Clicked"/>
        <ToolbarItem Order="Primary" Text="Edit" Clicked="Edit_Clicked"/>
        <ToolbarItem Order="Primary" Text="Add" Clicked="Add_Clicked"/>
    </ContentPage.ToolbarItems>
    
    <Grid>
        <Image Source="background.jpg" Aspect="AspectFill"/>
        <StackLayout>
        <!-- Place new controls here -->
            <Label Text="Activities" HorizontalOptions="CenterAndExpand" Font="Italic, Bold, Large" TextColor="White"/>

            <Grid x:Name="menuButtons">
                
            </Grid>
        </StackLayout>
        </Grid>

</ContentPage>
