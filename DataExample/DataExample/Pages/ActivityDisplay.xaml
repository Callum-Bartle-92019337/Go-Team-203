<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DataExample.Pages.ActivityDisplay">

    <ContentPage.Content>
        <Grid>
            <Image Source="background.png" Aspect="AspectFill" />
            <ScrollView>
                <StackLayout Opacity="1">
                    <Frame Margin="2" Padding="2" BackgroundColor="#7FFFFFFF">
                        <Label Text="{Binding Title}" TextColor="Black" Font="Bold, Large" />
                    </Frame>
                    <Image Source="{Binding Photo} " />
                    <Frame Margin="3" Padding="4" BackgroundColor="#7FFFFFFF">
                        <Label Text="{Binding Content}" TextColor="Black"/>
                    </Frame>
                </StackLayout>
            </ScrollView>
        </Grid>
    </ContentPage.Content>
</ContentPage>