<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DataExample.MenuMerchandiser">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Merchandisers"
                   HorizontalOptions="CenterAndExpand" />
            <Button Text="Merchandiser 1" Clicked="Merchandiser1_OnClicked"/>
            <Button Text="Merchandiser 2" Clicked="Merchandiser2_OnClicked"/>
            <Button Text="Merchandiser 3" Clicked="Merchandiser3_OnClicked"/>

            <!--<ListView x:Name="notesListView" ItemSelected="NotesListView_OnItemSelected" SelectedItem="{Binding}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Name}" Detail="{Binding Author}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>-->

        </StackLayout>
    </ContentPage.Content>
</ContentPage>